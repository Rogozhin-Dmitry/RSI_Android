<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.RSI" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/bluat_red</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RSI" parent="Base.Theme.RSI" />

    <style name="SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splashscreen</item>
        <item name="android:statusBarColor">@color/bluat_red</item>
    </style>

    <style name="Theme.RSI.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RSI.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RSI.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>